export interface Pensamento{
  //devemos passar todos os atributos esperados
  //feito para padronizar e garantir a integridade dos dados
  id?: string,
 conteudo: string,
  autoria: string,
  modelo: string
}
